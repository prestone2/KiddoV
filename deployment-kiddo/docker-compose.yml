version: '3.8'

services:
  kiddoverse-frontend:
    build:
      context: ../Frontend
      dockerfile: Dockerfile
    container_name: kiddoverse-frontend
    ports:
      - "1000:80"
    restart: unless-stopped
    networks:
      - kiddoverse

networks:
  kiddoverse:
    driver: bridge
